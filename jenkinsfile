pipeline {
    agent any

    stages {
        stage('Checkout') {
            steps {
                git 'https://github.com/ansh8898/Contact-Management-System.git'
            }
        }

        stage('Build') {
            steps {
                sh 'echo "Building the project..."'
            }
        }
    }
}
